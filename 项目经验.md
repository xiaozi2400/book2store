# 项目经验

## Claude Code Skill 开发经验

### 问题描述

auto-commit Skill 执行时，LLM 跳过了中间步骤（生成提交日志、展示改动摘要），直接显示批准提示。

### 根因

Skill 的 Steps 是描述性指引而非强制协议。LLM 将其理解为"建议步骤"而非"必须依次执行的流程"。

### 优化方案

使用 **Prerequisite + Checkpoint + 禁止约束** 强制执行顺序：

```markdown
### Step 2: 生成提交日志
- **Prerequisite**: Step 1 完成
- **Checkpoint**: 必须生成日志文本后才能继续，**禁止跳过此步骤**

### Step 3: 展示完整摘要
- **Prerequisite**: Step 1 和 Step 2 完成
- **Checkpoint**: 必须展示全部三项内容后才能进入批准，**禁止直接跳到批准提示**

### Step 4: 等待批准
- **Prerequisite**: Step 3 全部完成
- **Constraint**: **禁止**在 Step 3 完成前显示此提示
```

### 核心技巧

1. **Prerequisite**：明确前置步骤
2. **Checkpoint**：验证步骤完成的检查点
3. **禁止约束**：使用 `**禁止**` 等强调性词汇
4. **Edge Cases**：处理边界情况（无改动、用户拒绝等）
